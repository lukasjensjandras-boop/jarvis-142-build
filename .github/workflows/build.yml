name: Build JARVIS 16.2 S26 Right Hand

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: jarvis-162-s26-right-hand
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

    steps:
      - uses: actions/checkout@v4

      - name: Fetch private JARVIS 16 source bridge
        shell: bash
        run: |
          set -euo pipefail
          rm -rf JARVIS
          mkdir -p JARVIS
          for i in 1 2 3 4 5 6 7; do
            curl -fL --retry 3 --retry-delay 2               "https://hlrduovwbahcnqguqitw.supabase.co/functions/v1/jarvis-build-150-g${i}"               -o "/tmp/jarvis-g${i}.json"
          done
          python3 - <<'PY'
          import json, base64
          from pathlib import Path
          root=Path("JARVIS")
          count=0
          for i in range(1,8):
              data=json.loads(Path(f"/tmp/jarvis-g{i}.json").read_text())
              for item in data["files"]:
                  p=root/item["path"]
                  p.parent.mkdir(parents=True, exist_ok=True)
                  p.write_bytes(base64.b64decode(item["data"]))
                  count += 1
          print("Imported files:", count)
          assert count == 87, count
          PY
          grep -n 'versionName "16.2.0"' JARVIS/app/build.gradle
          grep -n 'compileSdk 36' JARVIS/app/build.gradle
          grep -n 'targetSdk 36' JARVIS/app/build.gradle

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Accept Android SDK licenses
        run: yes | /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager --licenses >/dev/null || true

      - name: Install Android API 36
        run: /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-36" "build-tools;35.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build APK and AAB
        working-directory: JARVIS
        run: gradle :app:assembleDebug :app:bundleDebug --stacktrace

      - name: Prepare JARVIS 16 files
        run: |
          cp JARVIS/app/build/outputs/apk/debug/app-debug.apk JARVIS-16.2-S26-Right-Hand-debug.apk
          cp JARVIS/app/build/outputs/bundle/debug/app-debug.aab JARVIS-16.2-S26-Right-Hand-debug.aab
          sha256sum JARVIS-16.2-S26-Right-Hand-debug.apk JARVIS-16.2-S26-Right-Hand-debug.aab > JARVIS-16.2-S26-Right-Hand-SHA256SUMS.txt
          ls -lh JARVIS-16.2-S26-Right-Hand-debug.apk JARVIS-16.2-S26-Right-Hand-debug.aab JARVIS-16.2-S26-Right-Hand-SHA256SUMS.txt

      - name: Upload JARVIS 16 artifact
        uses: actions/upload-artifact@v4
        with:
          name: JARVIS-16.2-S26-Right-Hand-package
          path: |
            JARVIS-16.2-S26-Right-Hand-debug.apk
            JARVIS-16.2-S26-Right-Hand-debug.aab
            JARVIS-16.2-S26-Right-Hand-SHA256SUMS.txt
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

      - name: Publish JARVIS 16 release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view jarvis-16-s26-build --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload jarvis-16-s26-build JARVIS-16.2-S26-Right-Hand-debug.apk JARVIS-16.2-S26-Right-Hand-debug.aab JARVIS-16.2-S26-Right-Hand-SHA256SUMS.txt --repo "$GITHUB_REPOSITORY" --clobber
          else
            gh release create jarvis-16-s26-build JARVIS-16.2-S26-Right-Hand-debug.apk JARVIS-16.2-S26-Right-Hand-debug.aab JARVIS-16.2-S26-Right-Hand-SHA256SUMS.txt               --repo "$GITHUB_REPOSITORY" --target main               --title "JARVIS Android 16.2 S26 Right Hand"               --notes "JARVIS 16.0.0 S26/Life OS debug APK and AAB."
          fi
