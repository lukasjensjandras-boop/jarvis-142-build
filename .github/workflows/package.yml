name: Package JARVIS Legacy (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy JARVIS 14.2 packaging is disabled. JARVIS 14.3 is built by build.yml."
